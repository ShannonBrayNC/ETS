FROM node:22-alpine AS build

WORKDIR /ui

COPY ets/explorer-ui/package.json ets/explorer-ui/package-lock.json ./
RUN npm ci

COPY ets/explorer-ui/ ./

# Keep Edge Virtual additive: the shared Explorer remains the default for non-Edge
# builds. The Edge image selects the hardened Dark Pro operator shell at build time.
ARG VITE_ETS_API_BASE_URL=
ARG VITE_ETS_EDGE_DARK_PRO=true
ENV VITE_ETS_API_BASE_URL=${VITE_ETS_API_BASE_URL}
ENV VITE_ETS_EDGE_DARK_PRO=${VITE_ETS_EDGE_DARK_PRO}

RUN npm run build

FROM nginx:1.27-alpine

COPY edge-demo/nginx.conf /etc/nginx/conf.d/default.conf
COPY --from=build /ui/dist /usr/share/nginx/html

EXPOSE 80
