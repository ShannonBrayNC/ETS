# ETS Capture v1 Test Plan

Status: GATE-G1B candidate
Parent: #227
SignalForge: Lantern-Protocol/SignalForge#54

## Contract validation

1. Validate `schemas/capture/v1/capture-envelope.schema.json` as Draft 2020-12.
2. Validate `schemas/capture/v1/examples/minimal.json` successfully.
3. Reject `invalid-raw-evidence.json` because `privacy.contains_raw_evidence` is fixed to `false`.
4. Reject `invalid-missing-representation.json` because every digest must declare the committed representation.
5. Reject missing tenant/workspace scope.
6. Reject tenant/workspace identifiers longer than the frozen `EvidenceEvent` v1 128-character limit.
7. Reject unknown root and nested fields.
8. Reject empty strings for optional identity/version/reference fields when those fields are present.
9. Reject digest algorithms other than SHA-256 and malformed digest hex.
10. Reject naive/non-date-time receipt timestamps in the runtime model.
11. Reject runtime `metadata` or `extensions` whose serialized UTF-8 JSON exceeds 16 KiB independently.
12. Reject non-JSON-native and non-finite values in runtime metadata/extension mappings.

## Compatibility

- Historical `schemas/edge/v1/capture-envelope.schema.json` SHA must remain unchanged from the G1B base commit.
- Historical schema identity remains `ets.edge.capture.v1`.
- `ets.event.v1` hashable fields and compatibility vectors remain unchanged.
- `ets.capture.*` must not import `ets.edge.*` or `ets.gateway.*`.
- Capture-to-event mapping may import protocol models only through `ets.core.api`.

## Mapping tests

Given one validated capture envelope and fixed caller-supplied event/evidence identifiers, repeated mapping must produce the same `EvidenceEvent` payload.

The mapped event must use:

- `source.tenant_id` -> `tenant_id`;
- `source.workspace_id` -> `workspace_id`;
- `transformation.output_event_type` -> `event_type`;
- `content_digest.value` -> `content_hash`;
- literal `sha256` -> `content_hash_alg`;
- `received_at_utc` -> `created_at_utc`;
- `source.system` -> `source_system`;
- capture `correlation_id` -> event `correlation_id`;
- `privacy.redaction_profile` -> event `redaction_profile`.

`observed_at_utc` must remain provenance metadata and must not replace receipt time.

The mapped metadata must preserve collector/adapter identity, source identifier, optional sequence/idempotency information, transport and declared identities as distinct values, clock quality, declared representation, custody reference, transformation, privacy state, and bounded extension metadata.

A maximum-bound runtime envelope must still map into an `EvidenceEvent` whose canonical metadata is within the frozen 64 KiB Core limit. If that invariant cannot be demonstrated, the capture bounds must be reduced rather than weakening the Core contract.

## Claims boundary tests

No API/result generated by this slice may represent a valid mapped event or proof as proof of:

- semantic truth;
- complete observation;
- regulatory compliance;
- legal admissibility.

## Exact-head gate

Before merge, require at minimum the SignalForge-registered ETS CI checks, Ruff, mypy, full pytest, Security Audit when available, unchanged v1 compatibility vectors, and independent review. PR #228 remains draft until the executable runtime/model layer is present and all of these gates pass on the exact reviewed head.
