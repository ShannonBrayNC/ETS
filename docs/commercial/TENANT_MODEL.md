# Tenant Model

The RC API is tenant-aware through `tenant_id`, `workspace_id`, headers, and JWT
claims. Hosted mode must add durable tenant accounts, role-based access control,
API keys or OAuth clients, audit retention, and data export controls.
