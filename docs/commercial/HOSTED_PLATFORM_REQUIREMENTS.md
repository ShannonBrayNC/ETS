# Hosted Platform Requirements

ETS Cloud is future work. It requires tenant accounts, production auth,
metering, rate limits, backups, support runbooks, signing key operations,
retention policies, and legal/compliance review.

The open reference implementation should remain usable without hosted services.
