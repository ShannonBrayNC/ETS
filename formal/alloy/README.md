# ETS Alloy Models

This directory contains bounded relational models for ETS causal evidence
semantics. The models are intended for research review and are deliberately
small enough to inspect.

Use Alloy Analyzer to open `ETSCausalModel.als`, then run the included checks.
When Alloy is unavailable in CI, tests verify that the model artifact exists and
contains the expected predicates and assertions.
